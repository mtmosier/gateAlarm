from distutils.core import setup

setup (name = 'gateAlarm',
       version = '0.1',
       description = 'Extension to interface with sensors and send sms messages.',
       packages = ['gateAlarmLib'])

